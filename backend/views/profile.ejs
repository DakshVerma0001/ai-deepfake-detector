<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
</head>
<body>
    

  <img src="<%= user.profilepic ? user.profilepic : '/images/uploads/default.jpg' %>" 
       alt="Profile pic" width="120" height="120">
<p>User ID: <%= user.customId %></p>
  <p>Name: <%= user.name %></p>
  <p>Email: <%= user.email %></p>
 <p>Address: <%= user && user.address ? user.address : "Not Provided" %></p>
  <p>Role: <%= user.role %></p>

  <a href="/profile/edit">Edit Profile</a>
<a href="/logout">logout</a>
    <h2>Your Issues</h2>
    <div class="issues-container">
        <% if (issues && issues.length > 0) { %>
            <% issues.forEach(issue => { %>
                <div class="issue-card">
                    <% if (issue.image) { %>
                        <img src="<%= issue.image %>" alt="Issue Image" width="200">
                    <% } %>
                    <h3><%= issue.title %></h3>
                     <p><%= issue.description %></p>
                   
                     <p><b>Location:</b> <%= issue.location %></p>
    <% if (issue.latitude && issue.longitude) { %>
      <a target="_blank" href="https://www.google.com/maps?q=<%= issue.latitude %>,<%= issue.longitude %>">
        📍 View on Google Maps
      </a>
    <% } %>
                    <p><strong>Status:</strong> <%= issue.status || "Pending" %></p>
               <a href="/issue/edit/<%= issue._id%>">edit</a>

                <form action="/issue/delete/<%= issue._id %>" method="POST" style="display:inline;">
          <button type="submit" 
                  style="padding:5px 10px; background:red; color:white; border:none; border-radius:5px; cursor:pointer;">
            🗑️ Delete
          </button>
        </form>
                </div>
            <% }) %>
        <% } else { %>
            <p>No issues posted yet.</p>
        <% } %>
    </div>
</body>
</html>
